{
  "name": "network-information-types",
  "version": "0.1.0",
  "description": "Type definitions for Network Information API",
  "types": "index.d.ts",
  "repository": "git@github.com:lacolaco/network-information-types.git",
  "author": "Suguru Inatomi <suguru.inatomi@gmail.com>",
  "license": "MIT",
  "scripts": {
    "build": "pack build",
    "test": "tsc -p tsconfig.spec.json",
    "preversion": "yarn build"
  },
  "@pika/pack": {
    "pipeline": [
      [
        "@pika/plugin-build-types"
      ]
    ]
  },
  "peerDependencies": {
    "typescript": ">= 3.0.0"
  },
  "devDependencies": {
    "@pika/pack": "^0.3.7",
    "@pika/plugin-build-types": "^0.3.16",
    "@pika/plugin-standard-pkg": "^0.3.16",
    "typescript": "^3.4.5"
  }
}
